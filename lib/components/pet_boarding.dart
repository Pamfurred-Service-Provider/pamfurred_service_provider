import 'package:flutter/material.dart';

class PetBoardingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Pet Boarding Services')),
      body: Center(
        child: Text('Pet Boarding Services'),
      ),
    );
  }
}
